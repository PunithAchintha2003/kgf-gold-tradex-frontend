import React from 'react';

interface PredictionExplanationProps {
  isDark: boolean;
}

const PredictionExplanationComponent: React.FC<PredictionExplanationProps> = () => {
  // Component removed - no longer showing prediction explanations
  return null;
};

export default PredictionExplanationComponent;
